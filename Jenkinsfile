pipeline {
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'java -version'
                sh 'echo "Deepak"'
            }
        }
    }
}